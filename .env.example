# Repo-root deployment defaults (copy to `.env` on the NAS after clone; never commit `.env`).
# `scripts/init-nas.sh` overwrites `STACK_ROOT` with the resolved stacks root on first run.
STACK_ROOT=/dockge/stacks
PUID=0
PGID=0
