# INVENTORY — acme-sh

**Path:** `acme-sh/compose.yaml` · **Generated:** auto via `docs/hive/tools/inventory.py`

## Services

| Name | Container | Image | Pin | Ports | Notes |
|---|---|---|---|---|---|
| `acme-sh` | `AcmeSh` | `neilpang/acme.sh:latest` | ✗ `:latest` | n/a | `network_mode: host` |

## Volumes

| Service | Host | Container | Mode |
|---|---|---|---|
| acme-sh | `/volume1/docker/dockge/stacks/acme-sh/data` | `/acme.sh` | (default) |
| acme-sh | `/volume1/certs/acme` | `/volume1/certs/acme` | (default) |

## Networks

No top-level `networks:` declared — services use compose-default project bridge.

## Secrets surface (keys only — no values)

Service environment keys:

| Service | Key | Source |
|---|---|---|
| `acme-sh` | `TZ` | inline `America/New_York` |
| `acme-sh` | `CF_Token` | inline `${CF_Token}` |
| `acme-sh` | `DISCORD_WEBHOOK_URL` | inline `${DISCORD_WEBHOOK_URL}` |

`.env` present: YES; `.env.example` present: no.

## Hostname check (per acme-sh/AGENTS.md boundary-aware regex)

**STALE hits** (`(?<!ots)orundscore`):

- `acme-sh/AGENTS.md:- `[2026-04-29]` For hard-cutover identifier/domain renames where the new token contains the old one (`orundscore` → `otsorundscore`), do content replacement and file/dir renames in one pass, then verify stale-token removal with boundary-aware checks (`rg '\borundscore'`, `rg --pcre2 '(?<!ots)orundscore'`, plus `rg 'otsotsorundscore'`) before running `/Volumes/certs/acme/tests/run-all.bash`.`
- `acme-sh/AGENTS.md:- `[2026-04-29]` Renaming ACME directory/file labels from `orundscore` to `otsorundscore` without re-issuing certs leaves X.509 SAN/CN payloads and `Le_Link*` order URLs bound to the old domains; treating this as a full cert cutover failed—perform a fresh `acme.sh --issue` after DNS is live instead of relying on rename-plus-renew.`
- `acme-sh/AGENTS.md:- `[2026-04-29]` Naive `rg orundscore` sweeps after the rename can produce false positives because `otsorundscore` contains the old token; use boundary/negative-lookbehind checks for stale-token validation to avoid chasing non-issues.`
- `acme-sh/SETUP.md:Otsorundscore DSM/Docker PEMs are staged **on a Mac** with `deploy-otsorundscore.bash` after`

## Gaps vs baseline ([_baseline/PROPOSAL.md](../_baseline/PROPOSAL.md))

| Item | `acme-sh` |
|---|---|
| `security_opt: no-new-privileges:true` | ✓ |
| `restart: unless-stopped` | ✓ |
| watchtower label | ✓ |
| `mem_limit` | ✓ `128m` |
| `cpu_shares` | ✓ `256` |
| `TZ` env | ✓ |
| image pin | ✗ `:latest` |
| `healthcheck` | ✗ missing |
| `logging` block | ✗ missing |

Stack-level:

- `.env.example`: ✗ missing
- `README.md`: ✗ missing
- Other files in stack folder: `AGENTS.md`, `SETUP.md`, `data.bak-20260428041540.zip`

## Notes

Auto-generated by `docs/hive/tools/inventory.py`. Stack-specific narrative (scope questions, RACI follow-ups, action priority) belongs in this stack's `PROPOSAL.md` — see [`../_baseline/PROPOSAL.md`](../_baseline/PROPOSAL.md) for the fleet-wide standard.
