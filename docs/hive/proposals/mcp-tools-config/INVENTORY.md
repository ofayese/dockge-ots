# INVENTORY — mcp-tools-config

**Path:** `stacks/mcp-tools-config/docker-mcp.yaml` (catalog, not Compose)

## Files

`docker-mcp.yaml` — Docker Desktop MCP catalog; not processed by `scripts/compose-validate.sh`.

## Operator data

YAML fragments (`config.yaml`, `registry.yaml`, …) — keep secrets out of git.
