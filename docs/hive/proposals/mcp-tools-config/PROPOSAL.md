# PROPOSAL — mcp-tools-config

**Owner:** operator · **2026-04-30**

## Summary

Document **`docker-mcp.yaml`** as a **non-Compose** catalog in `COMPOSE_FILENAMES.md` and stack README; no `docker compose` validation until/if converted to a real Compose project.

## Rollback

N/A (documentation-only classification).
