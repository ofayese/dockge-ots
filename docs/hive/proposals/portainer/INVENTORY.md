# INVENTORY — portainer

**Path:** `portainer/compose.yaml` · **Generated:** auto via `docs/hive/tools/inventory.py`

## Services

| Name | Container | Image | Pin | Ports | Notes |
|---|---|---|---|---|---|
| `portainer` | `portainer` | `portainer/portainer-ce:alpine-sts` | ✗ floating tag | `9000:9000`, `9443:9443` | — |
| `portainer_agent` | `portainer_agent` | `portainer/agent:2.39.1` | ✓ semver tag | `10.0.1.15:9001:9001` | caps drop=[ALL] add=[NET_RAW] |

## Volumes

| Service | Host | Container | Mode |
|---|---|---|---|
| portainer | `/var/run/docker.sock` | `/var/run/docker.sock` | (default) |
| portainer | `/volume1/docker/portainer` | `/data` | (default) |
| portainer_agent | `/var/run/docker.sock` | `/var/run/docker.sock` | (default) |
| portainer_agent | `/volume1/@docker/volumes` | `/var/lib/docker/volumes` | (default) |
| portainer_agent | `/` | `/host` | (default) |
| portainer_agent | `/volume1/docker/portainer/certs` | `/certs` | ro |

## Networks

No top-level `networks:` declared — services use compose-default project bridge.

## Secrets surface (keys only — no values)

Service environment keys:

| Service | Key | Source |
|---|---|---|
| `portainer` | `https://10.0.1.15:9001` | host env (no default) |
| `portainer_agent` | `AGENT_CLUSTER_ADDR` | inline `0.0.0.0` |
| `portainer_agent` | `AGENT_TLS_CERT` | inline `/certs/cert.pem` |
| `portainer_agent` | `AGENT_TLS_KEY` | inline `/certs/key.pem` |

`.env` present: YES; `.env.example` present: YES.

## Hostname check (per acme-sh/AGENTS.md boundary-aware regex)

`rg --pcre2 '(?<!ots)orundscore'`: **0 hits.** No stale hostnames.

## Gaps vs baseline ([_baseline/PROPOSAL.md](../_baseline/PROPOSAL.md))

| Item | `portainer` | `portainer_agent` |
|---|---|---|
| `security_opt: no-new-privileges:true` | ✓ | ✗ missing |
| `restart: on-failure:5` | variant `always` | variant `always` |
| watchtower label | ✓ | ✓ |
| `mem_limit` | ✗ missing | ✗ missing |
| `cpu_shares` | ✗ missing | ✗ missing |
| `TZ` env | ✗ missing | ✗ missing |
| image pin | ✗ floating tag | ✓ semver tag |
| `healthcheck` | ✗ missing | ✗ missing |
| `logging` block | ✗ missing | ✗ missing |

Stack-level:

- `.env.example`: ✓
- `README.md`: ✗ missing

## Auto-detected anomalies

- `portainer` env entry `https://10.0.1.15:9001` has no `=`; Compose treats it as a host-env name (likely malformed)
- `portainer` mounts `docker.sock` as `default rw` — consider `:ro` if container only needs to read
- `portainer_agent` mounts `docker.sock` as `default rw` — consider `:ro` if container only needs to read

## Notes

Auto-generated by `docs/hive/tools/inventory.py`. Stack-specific narrative (scope questions, RACI follow-ups, action priority) belongs in this stack's `PROPOSAL.md` — see [`../_baseline/PROPOSAL.md`](../_baseline/PROPOSAL.md) for the fleet-wide standard.
