# INVENTORY — searxng

**Path:** `searxng/compose.yaml` · **Generated:** auto via `docs/hive/tools/inventory.py`

## Services

| Name | Container | Image | Pin | Ports | Notes |
|---|---|---|---|---|---|
| `redis` | `SearXNG-Redis` | `valkey/valkey:8-alpine` | ✓ semver tag | n/a | — |
| `searxng` | `SearXNG` | `searxng/searxng:latest` | ✗ `:latest` | `10.0.1.15:8888:8080` | depends_on: redis (service_healthy); caps drop=[ALL] add=[CHOWN,SETGID,SETUID] |

## Volumes

| Service | Host | Container | Mode |
|---|---|---|---|
| searxng | `/volume1/docker/dockge/stacks/searxng/config` | `/etc/searxng` | rw |

## Networks

- `bridge` (external)

## Secrets surface (keys only — no values)

Service environment keys:

| Service | Key | Source |
|---|---|---|
| `searxng` | `SEARXNG_BASE_URL` | inline `https://search.otsorundscore.olutechsys.com/` |
| `searxng` | `SEARXNG_REDIS_URL` | inline `redis://redis:6379/0` |
| `searxng` | `UWSGI_WORKERS` | inline `4` |
| `searxng` | `UWSGI_THREADS` | inline `4` |

`.env` present: no; `.env.example` present: no.

## Hostname check (per acme-sh/AGENTS.md boundary-aware regex)

`rg --pcre2 '(?<!ots)orundscore'`: **0 hits.** No stale hostnames.

## Gaps vs baseline ([_baseline/PROPOSAL.md](../_baseline/PROPOSAL.md))

| Item | `redis` | `searxng` |
|---|---|---|
| `security_opt: no-new-privileges:true` | ✓ | ✓ |
| `restart: on-failure:5` | ✓ | ✓ |
| watchtower label | ✓ | ✓ |
| `mem_limit` | ✓ `256m` | ✓ `512m` |
| `cpu_shares` | ✓ `512` | ✓ `768` |
| `TZ` env | ✗ missing | ✗ missing |
| image pin | ✓ semver tag | ✗ `:latest` |
| `healthcheck` | ✓ | ✗ missing |
| `logging` block | ✗ missing | ✗ missing |

Stack-level:

- `.env.example`: ✗ missing
- `README.md`: ✗ missing

## Notes

Auto-generated by `docs/hive/tools/inventory.py`. Stack-specific narrative (scope questions, RACI follow-ups, action priority) belongs in this stack's `PROPOSAL.md` — see [`../_baseline/PROPOSAL.md`](../_baseline/PROPOSAL.md) for the fleet-wide standard.
