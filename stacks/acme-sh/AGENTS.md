# AGENTS Memory (Pointer)

This subdirectory defers to the canonical stack memory index:

- `../AGENTS.md`

Use the root `AGENTS.md` for durable learnings, policy updates, and recurring bug guidance.

Local note:

- [2026-04-30] Keep `acme-sh/AGENTS.md` as a pointer-only file to reduce drift; do not duplicate operational memory here.
