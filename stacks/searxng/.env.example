# Stack: searxng
# Tunables. Application secret_key lives in config/settings.yml (gitignored — see README).
TZ=America/New_York
UWSGI_WORKERS=4
UWSGI_THREADS=4

# Override if HAProxy frontend hostname changes:
SEARXNG_BASE_URL=https://search.otsorundscore.olutechsys.com/
