# Default TLS: PEMs from acme-sh (Cloudflare DNS-01). Optional Traefik resolver: see ../README.md.
tls:
  certificates:
    - certFile: /certs/misfitsds/fullchain.pem
      keyFile: /certs/misfitsds/privkey.pem
  stores:
    default:
      defaultCertificate:
        certFile: /certs/misfitsds/fullchain.pem
        keyFile: /certs/misfitsds/privkey.pem
