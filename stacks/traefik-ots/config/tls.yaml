# Default TLS: PEMs from acme-sh (Cloudflare DNS-01). Optional Traefik resolver: see ../README.md.
tls:
  certificates:
    - certFile: /certs/otsorundscore/fullchain.pem
      keyFile: /certs/otsorundscore/privkey.pem
  stores:
    default:
      defaultCertificate:
        certFile: /certs/otsorundscore/fullchain.pem
        keyFile: /certs/otsorundscore/privkey.pem
